---
// Hero Section - Landing introduction
import PageLoadAnimation from './PageLoadAnimation';
---

<section class="min-h-[calc(100vh-5rem)] md:min-h-[calc(100vh-4rem)] flex items-center justify-center py-20 sm:py-24 md:py-28 lg:py-20">
  <div class="w-full max-w-4xl">
    <div class="flex flex-col">
      <!-- Greeting -->
      <PageLoadAnimation client:load delay={800} variant="slide-up" distance={20}>
        <p class="text-[var(--color-accent-primary)] font-mono text-sm sm:text-base md:text-lg mb-5 sm:mb-6">
          Hi, my name is
        </p>
      </PageLoadAnimation>
      
      <!-- Name -->
      <PageLoadAnimation client:load delay={900} variant="slide-up" distance={20}>
        <h1 class="text-[clamp(2.5rem,8vw,4rem)] sm:text-5xl md:text-6xl lg:text-7xl font-bold text-[var(--color-text-primary)] leading-[1.1] mb-3 sm:mb-4">
          Pawel Sloboda.
        </h1>
      </PageLoadAnimation>
      
      <!-- Tagline -->
      <PageLoadAnimation client:load delay={1000} variant="slide-up" distance={20}>
        <h2 class="text-[clamp(1.75rem,6vw,3rem)] sm:text-4xl md:text-5xl lg:text-6xl font-bold text-[var(--color-text-secondary)] leading-[1.2] mb-6 sm:mb-8 md:mb-10">
          I build intelligent systems that solve real problems.
        </h2>
      </PageLoadAnimation>
      
      <!-- Description -->
      <PageLoadAnimation client:load delay={1100} variant="slide-up" distance={20}>
        <p class="max-w-2xl text-base sm:text-lg md:text-lg lg:text-xl text-[var(--color-text-secondary)] leading-[1.6] sm:leading-[1.7] mb-8 sm:mb-10 md:mb-12">
          I'm a software engineer who builds platforms that people actually use. I've helped uninsured families 
          find free healthcare, scaled learning apps to thousands of users, and built intelligence systems for 
          Fortune 500 companies. I write code that solves problems, not resumes.
        </p>
      </PageLoadAnimation>
      
      <!-- CTA Button -->
      <PageLoadAnimation client:load delay={1200} variant="slide-up" distance={20}>
        <div>
          <a 
            href="#projects"
            class="inline-block border-2 border-[var(--color-accent-primary)] text-[var(--color-accent-primary)] px-8 py-4 sm:px-9 sm:py-4 md:px-8 md:py-4 rounded-md font-medium text-base sm:text-base hover:bg-[var(--color-accent-primary)]/10 hover:shadow-[0_0_20px_rgba(167,139,250,0.3)] transition-all duration-300"
          >
            View My Work
          </a>
        </div>
      </PageLoadAnimation>
    </div>
  </div>
</section>
